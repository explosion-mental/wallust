[package]
name = "wallust"
version = "0.1.0"
edition = "2021"
license = "MIT"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
image = "0.24.*"
colorsys = "0.6"
clap = { version = "4.1.0", features = ["derive"] }
lab = "0.11.0"
histogram = "0.7.1"
owo-colors = "3"

# TODO drop-in replacement for `wal`
#[[bin]]
#name = "wal"
